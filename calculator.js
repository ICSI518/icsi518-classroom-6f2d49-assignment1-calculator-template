// Function to add two numbers
function add(a, b) {
  // return a + b;
}

// Example by dileep
const a = 10;
const b = 5;

// Display results on console
console.log(`Addition: ${a} + ${b} = ${add(a, b)}`);
